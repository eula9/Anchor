/**
 * 领域模型包。
 *
 * 存放与 UI 无关的纯业务数据模型，例如：
 * - Identity（今日身份，见 Identity.kt）
 * - Task（今日三件事条目，见 Task.kt）
 * - StreakInfo（连续行动天数信息，待实现）
 *
 * 具体模型类将在后续业务开发中定义。
 */
package com.example.anchor.domain.model
