package com.example.anchor

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * 本地单元测试示例（在开发机器 JVM 上运行）。
 */
class ExampleUnitTest {

    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
